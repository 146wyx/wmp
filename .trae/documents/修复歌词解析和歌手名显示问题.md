## 问题分析

1. **歌词时间标签不匹配** - 正则表达式要求毫秒是 2-3 位，但 `[00:00.0]` 只有 1 位，导致开头的元数据行无法解析

2. **歌手名显示 "-"** - 需要检查文件名解析逻辑

## 修复方案

1. **修复歌词解析正则表达式** (MusicPlayer.js)
   - 将 `(\d{2,3})` 改为 `(\d{1,3})`，支持 1-3 位毫秒

2. **检查歌手名解析** (server.py)
   - 确认 `parse_song_info` 函数能正确处理 "Too Deep-梓渝" 格式
   - 确保返回的 artist 不包含多余的 "-"

请确认后开始修复。